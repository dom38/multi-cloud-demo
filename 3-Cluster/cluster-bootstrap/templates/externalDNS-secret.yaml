apiVersion: core.karmada.io/v1alpha1
kind: KarmadaSecretPropagation
metadata:
  name: externalDNS-secret-propagation
spec:
  secretRef:
    name: externalDNS-secret
  placement:
    clusters:
    {{- .Values.clusters | toYaml | nindent 4 }}
